name: test

channels:
  - default

dependencies:
    # Base depends
  - python
  - pip
  - configargparse
  - statsmodels

    # Testing
  - coverage
  - flake8
  - jinja2
  - pytest
  - pytest-cov
  - pytest-runner

  # Pip-only installs
  - pip:
    - mendeleev
    - pint

      # Testing
    - codecov
    - yapf

