name: test

channels:
  - default
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip
  - statsmodels

  # SEAMM requirements
  - openbabel  # for molsystem

  # Testing
  - black
  - codecov
  - flake8
  - jinja2
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - mendeleev
    - molsystem>=2020.9.23
    - pint

    # Testing
    - yapf

    # Documentation
    - rinohtype
    - pygments
    - sphinx-rtd-theme
    - pystemmer
